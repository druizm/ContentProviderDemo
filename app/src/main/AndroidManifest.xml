<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="biz.belcorp.dev.contentproviderdemo">

    <!-- Permission BETA-PRODUCTION -->
    <uses-permission android:name="biz.belcorp.consultoras.esika.stage.provider.data.READ_DATABASE" />
    <uses-permission android:name="biz.belcorp.consultoras.lbel.stage.provider.data.READ_DATABASE" />

    <!-- Permission QAS -->
    <uses-permission android:name="biz.belcorp.consultoras.esika.develop.provider.data.READ_DATABASE" />
    <uses-permission android:name="biz.belcorp.consultoras.lbel.develop.provider.data.READ_DATABASE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>